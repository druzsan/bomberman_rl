[project]
name = "bomberman-rl"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.5.2",
    "pygame>=2.6.1",
    "tqdm>=4.70.0",
]

[project.optional-dependencies]
torch = [
    "torch>=2.13.0",
    "torchvision>=0.28.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.3",
]
